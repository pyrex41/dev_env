# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Local Development Environment - Safe Defaults
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# This file provides SAFE DEFAULT VALUES for local development only.
# These are NOT secure for production - use only for local dev!
#
# QUICK START:
#   cp .env.local.example .env && make dev
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# PostgreSQL Database Configuration
POSTGRES_DB=wander
POSTGRES_USER=wander_user
POSTGRES_PASSWORD=dev_password_postgres_123
POSTGRES_PORT=5432

# Redis Cache Configuration
REDIS_PASSWORD=dev_password_redis_456
REDIS_PORT=6379

# API Service Configuration
API_PORT=8000
DEBUG_PORT=9229
API_SECRET=dev_api_secret_key_for_local_only
JWT_SECRET=dev_jwt_secret_key_for_local_only

# Frontend Configuration
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:8000

# Environment Settings
NODE_ENV=development
